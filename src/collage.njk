---
layout: base
title: "Collage"
description: This is the Collage gallery
order: 3
tags: gallery
---

<h2>Collages</h2>

<div class="gallery-container">
    {% for image in visuals.collage %}
        <div class="gallery-item">
            <img src="{{ image.imgSrc }}" alt="{{ image.imgAlt }}" />
            <p class="image-caption">{{ image.caption }}</p>
        </div>
    {% endfor %}
</div>